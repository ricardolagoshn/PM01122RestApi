<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aplicacion.pm01122restapi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PM01122RestApi"
        android:usesCleartextTraffic="true">
        <activity android:name=".ActivityImagen"  >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".ActivityCrear">

        </activity>
        <activity android:name=".ActivityListaEmple"></activity>
        <activity android:name=".VolleyActivity" />
        <activity android:name=".ActivityRestApi" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>